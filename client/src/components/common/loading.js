import React from "react";
import loading from "../../images/svgs/cb-loader.svg";

const Loading = () => (
  <div>
    <img src={loading} alt="Loading..." className="cb-loader" />
  </div>
);

export default Loading;
